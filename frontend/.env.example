# ============================================================================
# ENVIRONMENT VARIABLES - Document Search UI
# ============================================================================
#
# PURPOSE:
#   Example environment configuration for the frontend application.
#   Copy this file to .env.local and customize for your environment.
#
# USAGE:
#   cp .env.example .env.local
#   # Edit .env.local with your settings
#
# IMPORTANT:
#   - .env.local is gitignored (safe for local secrets)
#   - Variables must start with VITE_ to be accessible in browser
#
# AUTHOR: AI Document Pipeline Team
# LAST UPDATED: October 2025
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------

# FastAPI backend URL
# DEVELOPMENT: http://localhost:8000
# PRODUCTION: Your deployed backend URL
VITE_API_URL=http://localhost:8000

# ----------------------------------------------------------------------------
# Feature Flags (Optional)
# ----------------------------------------------------------------------------

# Enable debug logging in browser console
# VITE_DEBUG=true

# Enable React Query DevTools
# VITE_ENABLE_DEVTOOLS=true

# ----------------------------------------------------------------------------
# Analytics (Optional)
# ----------------------------------------------------------------------------

# Google Analytics ID (if using)
# VITE_GA_ID=G-XXXXXXXXXX

# ----------------------------------------------------------------------------
# Deployment (Optional)
# ----------------------------------------------------------------------------

# Base path if deployed to subdirectory (e.g., /app)
# VITE_BASE_PATH=/

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
#
# 1. NEVER commit .env.local to git
# 2. Variables prefixed with VITE_ are embedded in client bundle
# 3. Don't put sensitive secrets here (visible in browser)
# 4. Vite automatically loads .env files in this order:
#    - .env.local (highest priority, gitignored)
#    - .env (committed to repo, default values)
#
